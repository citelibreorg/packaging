{
  "scripts": {
    "cy:common": "cypress run --headless --browser chrome --config specPattern=**/test-common-lutece.cy.js,video=true" ,
    "cy:clean": "rm -rf cypress/videos/ && rm -rf cypress/results/ || true"
  },
  "dependencies": {
    "mocha": "^11.7.5",
    "otplib": "^12.0.1"
  },
  "devDependencies": {
    "cypress": "^15.8.1",
    "cypress-multi-reporters": "^2.0.5",
    "mocha-junit-reporter": "^2.2.1"
  }
}
